# Scaffold Compiler V2.3 受信任配方注册方案

## 1. 目标与边界

V2.3 将一个内置配方分散在声明、答案规范化、交互问卷、候选装配、验证适配和运行时
上下文分派中的接线，收敛为一个可完整校验的受信任注册单元。编译器从同一注册集合派生
配方、问卷、装配、验证和运行时分派，消除“声明已加入但某一环漏注册”的半成品状态。

本版本不开放运行时插件、Python entry point、动态导入、清单命令、下载钩子或任意环境
变量转发。第三方配方包与签名信任模型需要独立协议，不与本次内部结构收敛混做。现有
FastAPI、CMake 和 Go 的配置、生成字节、验证门、恢复和自清理行为必须保持兼容。

## 2. 现状证据

- 新增配方目前要修改至少六类接线，运行时工厂按配方 ID 使用条件分支。
- 配方 ID、固定问卷和运行时分派相关硬编码分布于 15 个源码/测试文件、71 个引用点。
- 各底层注册表已经能够拒绝重复适配键和越界验证门，但没有一个上层原子边界证明同一
  配方的全部组成部分同时存在且身份一致。
- 蓝图清单已经是严格声明式数据；无需改变蓝图、生成事务或删除所有权模型。

## 3. 七类风险

- Invariants：每个配方必须恰有一个答案规范化器、问卷、装配适配器、验证适配器和运行时
  工厂；任一键或配方身份不一致都必须在应用构建时失败。
- Atomicity：注册集合要么完整编译成所有运行时注册表，要么不产生可运行应用，不能部分生效。
- Concurrency：注册编译发生在进程启动期且结果不可变；生成期不新增共享可变状态。
- Side effects：注册验证不得读写项目、启动工具或访问网络；问卷仍只原子写入配置文件。
- Trust boundaries：注册单元只能由胶囊内审查过的 Python 代码构造；声明式清单不能指定
  callable、模块路径、命令或下载位置。
- Observability：应用启动记录注册配方数量和稳定 ID；失败只报告稳定错误码，不泄露路径、
  环境或 callable 表示。
- Performance：启动时对有界注册集合做线性校验；生成热路径继续使用不可变字典/元组查找。

## 4. 宏观模块

1. 受信任注册合同：聚合一个配方的声明、答案、问卷、装配、验证和运行时能力。
2. 注册编译器：验证跨组件身份与唯一性，并一次性产出全部不可变注册表。
3. 通用交互入口：从已编译问卷注册表列出配方，不再持有语言 ID 常量。
4. 通用运行时分派：按已验证配方身份查找运行时工厂，不再使用语言条件分支。
5. 内置配方组合：FastAPI、CMake 和 Go 分别提供完整注册单元，根组合只聚合集合。
6. 一致性证据：合成配方失败测试、三配方等价测试、黄金项目和真实胶囊生命周期。

构建顺序：失败合同 → 注册编译器 → 问卷注册 → 运行时分派 → 三配方迁移 → 等价与文档。

会使方案失效的假设：不同配方必须共享同一个有状态运行时工厂；配方装配必须在注册后
动态改变；现有 V1 FastAPI 兼容路径无法包装为注册单元。任一成立都返回架构评审，不能
通过暴露动态导入或弱化注册校验绕过。

## 5. 架构决定

ADR-V23-REGISTRATION-1
  Decision : 使用胶囊内受信任代码构造的完整配方注册单元，不开放运行时插件。
  Context  : 通用化需要降低新增语言接线成本，但任意插件会扩大代码执行、依赖和删除信任边界。
  Options  : 继续多点手工注册；开放动态插件；使用闭合受信任注册单元。
  Chosen   : 闭合注册单元，由发布资产和代码审查共同建立信任。
  Tradeoff : 用户不能临时安装未审计配方；维护者新增配方更一致且安全边界不变。
  Rollback : 恢复现有内置组合根；`v2.2.0` 稳定资产保持不变。

ADR-V23-REGISTRATION-2
  Decision : 从同一注册集合原子编译全部注册表，并在应用启动期失败关闭。
  Context  : 分散注册可能形成声明存在但问卷、验证或运行时缺失的半配置状态。
  Options  : 首次使用时逐项报错；启动期分别构建；一次性跨组件校验并编译。
  Chosen   : 一次性验证配方 ID、适配键、问卷和运行时工厂的完整绑定；配方允许请求的验证门
             必须全部由适配器覆盖，适配器可额外报告固定的内部安全与清理门。
  Tradeoff : 启动多一次有界校验；换取运行前确定性和更小故障面。
  Rollback : 保留底层注册表接口，可临时恢复旧组合而不改生成事务。

ADR-V23-REGISTRATION-3
  Decision : 交互问卷与运行时工厂都通过不可变注册表按配方身份分派。
  Context  : 当前问卷元组和运行时条件分支是新增配方最容易遗漏的两个接线点。
  Options  : 保留条件分支；把命令写入清单；注册受信任 callable。
  Chosen   : callable 只能由内置注册单元提供，声明仅引用闭合稳定键。
  Tradeoff : 仍需编写受审查 Python 适配代码；不引入任意清单执行能力。
  Rollback : 还原固定问卷和条件分支，配置 schema 与成品不受影响。

## 6. 微任务

[MICRO PLAN]

Phase 1 - 完整注册合同
  Task 1.1 | 失败测试
    Input    : 缺失组件、错配适配键、重复配方/问卷/运行时键和越界验证门。
    Output   : 稳定错误码与零部分注册合同。
    Risk     : 信任边界、原子性。
    Rollback : 只删除新增测试。

  Task 1.2 | 最小注册编译器
    Input    : Task 1.1。
    Output   : 不可变注册单元、跨组件校验和一次性编译结果。
    Risk     : 注册身份或唯一性校验不完整。
    Rollback : 删除上层编译器，底层注册表保持原状。

Phase 2 - 通用入口
  Task 2.1 | 问卷注册失败测试与实现
    Input    : 已编译注册集合、合成配方问卷。
    Output   : `init` 只显示已注册配方并复用权威答案规范化器。
    Risk     : 配置兼容、原子写入。
    Rollback : 恢复固定问卷元组。

  Task 2.2 | 运行时分派失败测试与实现
    Input    : 配方身份、规范化配置、运行 ID 和受控工具上下文。
    Output   : 无语言条件分支的闭合运行时工厂查找。
    Risk     : 错配工具、环境泄漏、错误配方执行。
    Rollback : 恢复现有条件分支。

Phase 3 - 内置迁移
  Task 3.1 | 三配方注册单元
    Input    : 现有声明、规范化器、问卷、装配和验证适配器。
    Output   : FastAPI、CMake、Go 各自完整注册，组合根只聚合注册集合。
    Risk     : 行为漂移和 V1 兼容。
    Rollback : 逐配方恢复旧接线。

  Task 3.2 | 等价与故障证据
    Input    : 当前配置、黄金树、缺工具、错配注册和失败恢复场景。
    Output   : 三配方输出/验证等价、启动期失败关闭、生命周期不变。
    Risk     : 半注册或错误分派进入生成事务。
    Rollback : 阻止版本发布。

Phase 4 - 产品化
  Task 4.1 | 作者文档与版本
    Input    : 新注册合同和迁移证据。
    Output   : 单点注册作者流程、明确非插件边界和 `2.3.0-dev` 身份。
    Risk     : 文档鼓励绕过信任模型。
    Rollback : 恢复 V2.2 文档和版本。

  Task 4.2 | 全量与工程审查
    Input    : 完整实现。
    Output   : Ruff、mypy、全量测试、双平台 CI 和发现优先审查。
    Risk     : 架构漂移或跨平台回归。
    Rollback : 保留 `v2.2.0` stable，不创建 V2.3 标签。

Total tasks     : 8
Can parallelize : 文档草案可与等价测试并行；注册合同、入口迁移和组合根必须串行。

## 7. 状态

- Done：现状审查、架构方案以及 Phase 1 至 Phase 4；FastAPI、CMake、Go 各自拥有
  一个完整受信任注册模块，总聚合器只汇总注册单元，应用组合根只原子编译并注入全部结果。
  `init` 与运行时均不再持有语言分支，声明返回防御性副本，环境边界只捕获 `DATABASE_URL`。
- Tests：121 个文件格式检查、lint 和严格 mypy 通过；最终单次全量 325 passed/4 个环境相关测试
  跳过/275 个子场景。新证据覆盖三配方完整编译、三种运行时类型与工具绑定、问卷集合完整性、
  注入采集器、重复绑定、未知运行时配方、身份错配和固定内部验证门。
- Observability：编译入口只记录注册/成功数量或稳定错误码；运行时工厂记录开始、完成，失败
  只记录运行 ID、配方 ID 和异常类型，不记录环境、路径或异常正文。
- Review：最终工程审查无未解决的高、中、低风险发现；修正了验证适配器可报告固定内部门但必须
  覆盖配方全部允许门的真实合同。一次 Windows 进程启动抖动单测重跑与随后全量均通过。
- Next：保持 `2.3.0-dev` 为未发布开发身份；如需公开候选版，另行执行版本选择、确定性资产、
  精确提交 CI、标签和 GitHub Release 门禁，不移动 `v2.2.0` 稳定标签。
- Debt：第三方配方分发、签名和沙箱协议明确延期，不混入 V2.3。
- Rollback point：`v2.2.0` 稳定标签与资产不移动。
