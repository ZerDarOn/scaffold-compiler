# Scaffold Compiler V2.1 稳定版发布方案

## 1. 目标与边界

将已经完成资产级验收的 `v2.1.0-rc.2` 晋升为 `v2.1.0` 稳定版。稳定版只改变版本与
发布文档，不改变运行时代码、蓝图、生成结果、事务、安全清理或配方能力。

发布资格要求：RC2 后没有未解决 Issue/PR，精确稳定版提交通过本地全量与双平台 CI，
标签工作流重新构建并验证资产，下载资产摘要匹配且至少完成 Go 的真实生成、自清理和
生成后测试。

## 2. 七类风险

- Invariants：稳定版必须与 RC2 运行行为一致；标签必须精确绑定通过 CI 的版本提交且不可移动。
- Atomicity：版本、测试合同和 README 同一提交；只有该提交 CI 全绿后才创建标签。
- Concurrency：发布步骤严格串行，不允许两个标签或发布工作流竞争同一版本。
- Side effects：公开标签和 GitHub Release 是不可逆发布副作用，推迟到全部资格门通过之后。
- Trust boundaries：发布资产只能由受固定依赖保护的标签工作流生成，下载后独立校验摘要。
- Observability：保留主分支与标签工作流 ID、提交、资产摘要和真实验收结果。
- Performance：稳定版没有运行时改动；测试和资产审计成本有界，不影响生成项目。

## 3. 发布模块与顺序

1. 资格审查：核对公开 Issue、RC2 后提交和 CI，不允许隐含运行时变化。
2. 版本准备：同步包版本、版本测试、README 稳定版链接和构建示例。
3. 精确提交验证：本地质量门通过后提交、推送，并等待该提交的全部 CI。
4. 不可变发布：创建带注释的 `v2.1.0` 标签，由工作流构建并发布资产。
5. 独立资产验收：下载公开 ZIP 与摘要，验证真实生命周期和生成项目测试。
6. 证据收尾：记录工作流、摘要、结论和后续技术债，再次通过主分支 CI。

会使方案失效的假设：RC2 后出现未解决缺陷；稳定版提交包含版本/文档之外的运行时变化；
标签资产与本地确定性构建不一致。任一成立都阻止标签或终止稳定版发布。

## 4. 架构决定

ADR-V21-STABLE-1
  Decision : 从 RC2 运行基线创建独立稳定版版本提交，不移动或复用 RC2 标签。
  Context  : 稳定版需要无候选后缀的身份，同时必须保留已经审计的候选资产和回滚证据。
  Options  : 移动 RC2 标签；直接把 RC2 Release 改为正式；创建只含版本/文档的稳定提交。
  Chosen   : 创建稳定提交，通过精确提交 CI 后新建带注释的 `v2.1.0` 标签。
  Tradeoff : 产生一个只含发布元数据的提交，但版本身份、审计链和回滚边界清晰。
  Rollback : 标签前恢复版本准备提交；标签后不移动标签，保留 `v2.0.0` 为既有稳定回滚点。

## 5. 微任务

[MICRO PLAN]

Phase 1 - 资格与版本
  Task 1.1 | 公开状态审查
    Input    : RC2 发布、公开 Issue/PR、近期 CI、RC2 后提交。
    Output   : 无新增回归且运行代码未漂移的证据。
    Risk     : 带已知缺陷晋升。
    Rollback : 停止发布。

  Task 1.2 | 稳定版本准备
    Input    : 已验收 RC2 基线。
    Output   : `2.1.0` 包版本、测试合同和 README。
    Risk     : 版本身份与文档不一致。
    Rollback : 恢复版本准备变更。

Phase 2 - 精确提交门
  Task 2.1 | 本地质量验证
    Input    : 稳定版本准备树。
    Output   : 格式、lint、类型、全量测试和确定性胶囊证据。
    Risk     : 标签前遗漏回归。
    Rollback : 不提交或修复后重跑。

  Task 2.2 | 主分支 CI
    Input    : 单一稳定版本提交。
    Output   : 精确提交的双平台与真实发布组合 CI。
    Risk     : 本机环境掩盖跨平台错误。
    Rollback : 不创建标签。

Phase 3 - 发布与资产审计
  Task 3.1 | 标签与 GitHub Release
    Input    : CI 全绿的精确提交。
    Output   : 不可变 `v2.1.0` 标签和公开稳定版资产。
    Risk     : 公开不可逆副作用。
    Rollback : 不移动标签；失败时保留证据并修复为后续版本。
    Flag     : 创建公开 Git 标签与 GitHub Release。

  Task 3.2 | 下载资产验收
    Input    : 公开 ZIP 与 `.sha256`。
    Output   : 摘要匹配、真实生成、自清理和生成后测试证据。
    Risk     : 发布资产与开发树不等价。
    Rollback : 不宣告完成，保留 V2.0 稳定回滚点。

Phase 4 - 证据收尾
  Task 4.1 | 发布记录
    Input    : CI、Release 和资产审计结果。
    Output   : 可复核状态、剩余技术债和最终主分支 CI。
    Risk     : 发布事实不可追踪。
    Rollback : 仅恢复文档提交，已发布标签不移动。

Total tasks     : 7
Can parallelize : 无；所有发布副作用必须按门串行。

## 6. 状态

- Done：Tasks 1.1–2.1；公开状态、稳定版身份、测试合同、README 和本地质量门均已完成。
- Tests：格式检查 125 个文件、lint、111 个文件严格 mypy 全部通过；全量 296 passed、4 skipped。主机默认
  `%TEMP%` 首次运行被 Code Integrity 策略阻止未签名验证入口，事件 3033/3077 已确认；同一全量测试
  在仓库内受信任临时根通过，未修改产品或放宽安全边界。
- Next：Task 2.2，提交并等待精确稳定版本提交的主分支 CI。
- Debt：Windows 120 UTF-16 单元兼容护栏与固定 Go 1.22.12 升级策略留待后续版本评审。
- Rollback point：`v2.0.0`、`v2.1.0-rc.1`、`v2.1.0-rc.2` 标签均不移动。
