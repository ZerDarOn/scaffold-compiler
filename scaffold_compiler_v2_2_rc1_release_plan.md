# Scaffold Compiler V2.2 RC1 发布方案

## 1. 目标与边界

将已经通过双平台和真实发布组合验证的固定长度工作区实现冻结为
`2.2.0-rc.1`。候选版只改变版本身份和发布记录，不再改变运行时行为、蓝图、生成结果、
恢复协议或自清理边界；`v2.1.0` 继续作为最新稳定回滚点。

## 2. 风险与不变量

- 版本、标签和胶囊清单必须完全一致，标签只能指向通过精确提交 CI 的提交。
- 发布前的版本准备、质量门和资产构建必须串行；公开标签与 Release 最后执行。
- 资产只能由锁定依赖的标签工作流重建，下载后必须独立校验 SHA-256。
- 候选胶囊必须真实证明长目标名生成、固定长度工作区、自清理及生成后测试。
- 新旧失败工作区恢复兼容性不得因版本冻结发生变化。

## 3. 发布决定

ADR-V22-RC1-1
  Decision : 从已验证的 `2.2.0-dev` 主线创建独立 `2.2.0-rc.1` 版本提交和带注释标签。
  Context  : 固定长度工作区属于持久化会话和安全清理协议变化，需要候选资产观察期。
  Options  : 直接发布稳定版；继续无版本开发；先发布 RC1。
  Chosen   : 先发布 RC1，保持 `v2.1.0` 为稳定版。
  Tradeoff : 多一次资产审计，但保留清晰的兼容和回滚边界。
  Rollback : 标签前恢复版本提交；标签后不移动标签，修复进入后续候选版本。

## 4. 微任务

[MICRO PLAN]

Phase 1 - 资格与版本
  Task 1.1 | 公开状态和差异审查
    Output   : 无开放 Issue/PR、V2.1 后差异仅覆盖 V2.2 计划范围。
    Rollback : 任一阻断项出现即停止发布。

  Task 1.2 | RC1 版本冻结
    Output   : 包版本、测试合同、验收胶囊身份和 README 构建示例统一为 `2.2.0-rc.1`。
    Rollback : 恢复版本准备改动。

Phase 2 - 精确提交门
  Task 2.1 | 本地质量与确定性资产
    Output   : 格式、lint、mypy、测试、两次构建字节一致及摘要校验。
    Rollback : 不提交或修复后重跑。

  Task 2.2 | 主分支 CI
    Output   : 精确版本提交的 Windows、Ubuntu 和真实发布组合全部通过。
    Rollback : 不创建标签。

Phase 3 - 候选发布
  Task 3.1 | 标签和 GitHub prerelease
    Output   : 不可变 `v2.2.0-rc.1` 标签与公开候选资产。
    Rollback : 不移动公开标签；失败时发布后续候选。
    Flag     : 创建公开 Git 标签和 GitHub prerelease。

  Task 3.2 | 下载资产验收
    Output   : 摘要匹配、长目标名真实生成、自清理和生成后测试证据。
    Rollback : 不宣告候选完成，保持 `v2.1.0` 稳定回滚点。

## 5. 状态

- Done：Tasks 1.1–3.2；公开 Issue/PR 为零，最新稳定版为 `v2.1.0`，V2.1 后差异符合
  短工作区方案；RC1 版本身份已统一。本地 130 个文件格式检查、lint、114 个文件严格
  mypy、301 passed/1 个 POSIX 平台测试跳过，确定性胶囊与摘要合同通过。
- CI：精确版本提交 `9ace302` 的 quality `34318378515` 全绿；标签工作流
  `34318659954` 的 Windows、Ubuntu、Linux 真实组合、构建和发布任务全部成功。
- Release：`v2.2.0-rc.1` 已作为 prerelease 发布；公开 ZIP SHA-256 为
  `690e04b5a13c02ed85887c2144b14c591b650b95efaaa4fd6ab46814340fc025`。
- Asset audit：下载 ZIP 与校验文件匹配；下载胶囊以 89 字符目标叶完成 Go 配方真实生成、
  `.scw-*` 清理、胶囊自清理及生成后 `go test ./...`，临时审计目录已删除。
- Review：版本冻结未改变运行时；无未解决的高、中风险发现。
- Promotion：RC1 后没有公开缺陷或运行时代码漂移；`v2.2.0` 已完成稳定发布和公开资产验收。
- Rollback point：`v2.1.0` 标签与发布资产不移动。
