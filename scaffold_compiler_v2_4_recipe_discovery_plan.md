# Scaffold Compiler V2.4 配方发现与声明式问卷方案

## 1. 目标与边界

V2.4 让用户、脚本和上层工具在创建配置之前，确定性地发现编译器内置的项目配方及其公开
能力，并逐步让同一份受信任输入描述同时驱动交互问卷和机器可读目录。

本版本不开放第三方插件、动态导入、远程蓝图、任意命令或运行时下载。生成事务、Finalize、
恢复、自清理和无覆盖发布语义保持不变。

## 2. 当前问题

- `init` 能展示配方，但自动化必须模拟交互输入才能知道可选配方。
- 配方身份、前置工具和验证门已经位于受信任注册中，却没有只读 CLI 出口。
- 问卷提示仍是每种语言的手写代码，未来加入输入描述会有规则、提示和文档漂移风险。
- FastAPI 手写 JSON 的省略默认值是 `postgres + docker`，交互空输入默认值是
  `none + none`。这是已发布兼容行为，不能在没有显式决定和迁移说明时顺手统一。

## 3. 七类风险

- Invariants：发现输出只能来自成功原子编译的受信任注册；不得暴露可执行对象、绝对工具路径、
  环境变量值或秘密。
- Atomicity：任一配方的公开元数据无效时，整个注册编译失败，不得输出部分目录。
- Concurrency：发现命令只读，不创建锁、工作区、配置或清理监督进程。
- Side effects：`recipes` 不探测工具、不访问网络、不写文件；后续问卷生成仍只写显式配置目标。
- Trust boundaries：公开字段是固定数据，不接受配置、蓝图或命令行注入可执行描述。
- Observability：记录命令开始、结束和配方数量，不记录主目录、工具路径或环境内容。
- Performance：目录由内存中的已编译注册生成，复杂度随内置配方数线性增长，不扫描蓝图文件。

## 4. 架构决定

ADR-V24-1
  Decision : 增加无参数、始终输出确定性 JSON 的 `recipes` 命令。
  Context  : 当前只有交互式 `init` 能发现配方，不适合 CI、GUI 或智能代理调用。
  Options  : 解析帮助文本；读取蓝图目录；从已编译受信任注册输出结构化目录。
  Chosen   : 复用配方注册表和问卷注册表，输出 schema、编译器版本及有序配方列表。
  Tradeoff : 第一阶段只公开已有可信元数据，不立即承诺完整输入 schema。
  Rollback : 删除只读命令即可；不涉及生成项目或持久化格式迁移。

ADR-V24-2
  Decision : 输入描述符进入完整受信任配方注册，而不是蓝图清单或独立 UI 配置。
  Context  : 问卷包含可执行收集器，蓝图是非执行数据；二者必须保持清晰信任边界。
  Options  : 继续手写问卷；把提示放入蓝图；在受信任注册中声明字段并由通用收集器解释。
  Chosen   : 后续阶段增加严格的字符串、布尔和枚举描述符，并由注册编译器交叉校验。
  Tradeoff : 注册合同更丰富，但新增配方不再需要复制输入循环。
  Rollback : 保留已验证的答案 normalizer 为最终权威；描述符失败时注册整体拒绝。

ADR-V24-3
  Decision : 暂时显式区分解析默认值与交互默认值。
  Context  : FastAPI 两种入口已有不同默认行为，静默统一会改变已发布配置语义或向导体验。
  Options  : 立即统一为重型默认；统一为空能力默认；保留差异并在发现 schema 中标明来源。
  Chosen   : V2.4 先保留兼容行为，描述符阶段用明确字段表达交互默认，解析器仍是规范化权威。
  Tradeoff : 短期存在两类默认值，但不制造隐式破坏性变化。
  Rollback : 如后续决定统一，使用新配置 schema 或主版本兼容决定。

## 5. 微任务

[MICRO PLAN]

Phase 1 - 只读配方目录
  Task 1.1 | CLI 路由合同
    Input    : 无参数 `recipes` 命令。
    Output   : 调用服务的只读配方目录方法，拒绝未知参数。
    Risk     : 命令面与退出码兼容。
    Rollback : 移除新子命令和协议方法。

  Task 1.2 | 确定性目录应用服务
    Input    : 已编译配方与问卷注册表、编译器版本。
    Output   : schema 1 JSON，包含有序身份、标签、版本、前置条件、蓝图和验证 allowlist。
    Risk     : 元数据漂移、敏感运行时信息泄露、副作用。
    Rollback : 保留注册表，不暴露目录方法。

Phase 2 - 声明式输入描述
  Task 2.1 | 严格描述符领域模型
    Input    : 受限字符串、布尔、枚举字段声明。
    Output   : 不可变、可序列化、拒绝重复或无效组合的输入描述符。
    Risk     : 信任边界、默认值歧义。
    Rollback : normalizer 继续作为唯一解析权威。

  Task 2.2 | 完整注册原子编译
    Input    : 每个内置配方的输入描述符。
    Output   : 描述符身份和答案字段与完整注册共同验证，失败时不暴露部分结果。
    Risk     : 注册不完整、跨组件错绑。
    Rollback : 恢复到 Phase 1 的已有元数据目录。

  Task 2.3 | 通用问卷收集器
    Input    : 已编译描述符和公共项目字段。
    Output   : 三种现有配方保持字节等价配置输出，删除语言分支式输入循环。
    Risk     : 交互兼容、默认值变化。
    Rollback : 恢复现有受信任 collector，不改变 normalizer。

Phase 3 - 完整机器可读发现
  Task 3.1 | 目录输入 schema
    Input    : Phase 2 已编译描述符。
    Output   : `recipes` 增加字段类型、选择、必填性、交互默认和推导说明。
    Risk     : 对外 schema 稳定性、信息重复。
    Rollback : 保持 schema 1，后续字段通过新 schema 版本引入。

  Task 3.2 | 文档与胶囊验收
    Input    : 版本化一次性胶囊。
    Output   : 源码和胶囊中的发现输出一致；命令零文件系统副作用；用户和作者文档同步。
    Risk     : 打包遗漏、文档漂移。
    Rollback : 不进入候选发布。

Phase 4 - 发布资格
  Task 4.1 | 完整质量与工程评审
    Input    : V2.4 完整实现。
    Output   : Windows/Linux CI、真实组合、回归测试和最终风险记录。
    Risk     : 跨平台回归、未覆盖契约。
    Rollback : 保留 `v2.3.0` 为稳定回滚点。

Total tasks     : 8
Can parallelize : 无；公开 schema、注册编译和问卷迁移必须按序完成。

## 6. 状态

- Done：V2.3.0 已发布并通过公开资产生命周期验收；工作区从同一远端干净恢复。
- Done：Tasks 1.1、1.2。`recipes` 无参数路由、确定性 schema-1 JSON、三种内置配方顺序、
  发布入口不解析外部工具、运行时路径和环境值不泄露、零文件系统副作用均有测试；真实组合
  输出已核验。
- Tests：相关 CLI、应用服务、发布入口、文档和版本合同 37 passed、67 个子场景，相关 lint 和
  严格 mypy 通过。完整静态门为 142 个文件格式检查、lint、121 个源文件严格 mypy 通过；
  全量运行其余 325 passed、6 个环境跳过，M01 与 Windows 进程树终止两个宿主权限失败在
  仓库内隔离目录和宿主权限下分别复跑通过。
- Done：Tasks 2.1、2.2。输入描述符是严格、不可变、JSON 可序列化的受信任领域对象；三种
  内置配方已声明全部输入，完整注册编译会原子拒绝空描述、重复键和错误容器，不暴露部分结果。
- Done：Task 2.3。通用问卷工厂支持字符串、布尔和枚举，以及 reject、omit、literal 三种
  空值语义；三种内置配方已删除语言专用输入循环，逐字提示、重试和配置输出合同保持一致。
- Done：Task 3.1。`recipes` schema 2 按注册顺序输出同一份 `inputs`，包含类型、选择、必填性、
  空值语义、交互默认和推导提示；源码入口仍保持确定、只读和无运行时值泄漏。
- Done：Task 3.2。用户与作者文档已同步；真实构建胶囊与源码入口的 `recipes` stdout、stderr
  逐字一致，schema 2 输入顺序正确，携带私有环境值时无泄漏，执行前后文件快照一致。
- Current：Phase 4，功能开发完成，等待全量质量门和 CI 跨平台、真实组合验证。
- Next：运行完整格式、lint、严格 mypy、单元/验收测试，推送后核验全部 GitHub Actions。
- Debt：第三方配方签名、沙箱和分发协议继续延期；FastAPI 双默认行为等待显式兼容决定。
- Rollback point：`v2.3.0`。
