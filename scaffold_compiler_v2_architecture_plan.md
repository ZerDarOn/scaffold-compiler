# Scaffold Compiler V2 通用化架构蓝图

> 状态：宏观蓝图与微任务已确认；Phase 1 已完成
> 基线：V1 FastAPI 配方、原子交付与一次性胶囊保持可用
> 目标：证明同一生成器内核可生成并验证 Python/FastAPI 与 C/CMake 两类项目

## 1. 产品定义

Scaffold Compiler 是一个语言无关的一次性脚手架编译器：

```text
通用项目输入
+ 一个受信、内置、版本冻结的项目配方
+ 该配方允许的声明式蓝图组合
= 经过真实工具链验证、完全独立的目标项目
```

生成器只负责生成期。Finalize 后的项目不包含生成器、配方、蓝图、运行日志、工作区或升级元数据；一次性胶囊在外部监督下精确清理自身。生成器不长期管理已经交付的项目。

V2 的“通用”具有可验证边界：

- 编译生命周期、配置封装、蓝图协议、能力求解、路径安全、验证报告、原子交付和自清理不依赖具体语言；
- 每种项目类型由注册配方定义输入、允许能力、装配策略和验证策略；
- 配方与蓝图随编译器发布并冻结，不从网络下载，不执行清单提供的任意代码；
- 至少有 Python/FastAPI 与 C/CMake 两个差异足够大的配方通过同等级验收，才能宣布内核通用。

## 2. V2 范围

### 2.1 首批配方

| 配方 | 基础输出 | 可选能力 | 必需验证 |
|---|---|---|---|
| Python/FastAPI 服务 | 保持 V1 项目结构和行为 | PostgreSQL、Docker | 锁定安装、格式、Lint、类型、测试、应用健康及所选交付能力 |
| C/CMake CLI | 可执行程序、库边界、单元测试和 CMake 预设 | 严格编译警告 | 配置、编译、CTest、运行产物 |

C 配方的作用不是追求功能丰富，而是证明核心协议没有 Python、包名、`pyproject.toml`、`uv` 或 ASGI 假设。

### 2.2 V2 明确不做

- 第三方或远程配方市场；
- 配方清单中的 Shell、Python 导入路径或任意可执行钩子；
- 向已有或非空目录注入内容；
- Finalize 后的 add、remove、upgrade 或 doctor；
- 同时加入 Rust、Go、Node.js 等更多语言；
- 自动探测并静默选择宿主工具链；
- 降低 V1 的路径、摘要、无覆盖提交或自清理保证。

## 3. 七维领域风险

| 维度 | 约束与后果 |
|---|---|
| 不变量 | 配方只能选择其允许的蓝图和验证器；成品不得引用生成设施；旧 V1 配置必须继续产生等价 FastAPI 项目。破坏会造成跨技术栈污染或兼容性回归。 |
| 原子性 | 配方解析、能力计划、候选装配和验证凭证必须绑定同一摘要；最终发布继续使用现有无覆盖原子移动。任何阶段失败都不得产生部分目标。 |
| 并发 | 不同配方仍竞争同一目标路径锁；配方 ID 不得形成第二套锁域，否则两次生成可能同时写向同一目标。 |
| 副作用 | CMake、编译器、uv、数据库和 Docker 都是受控外部进程；必须有参数数组、超时、输出上限、秘密脱敏和进程树回收。 |
| 信任边界 | 用户配置与所有清单均不可信；配方只能引用编译器内注册的受信装配器和验证器键，不能把字符串升级为可执行代码。 |
| 可观测性 | 状态与验证报告必须记录配方 ID、配方版本、蓝图摘要、验证器和失败阶段；不得记录秘密或无界工具输出。 |
| 性能 | 多技术栈验证仍按一次生成串行执行；每个外部门有固定超时，不做跨配方预装或全局缓存，不允许蓝图数量导致无界求解。 |

最高风险是“为了插件化而允许清单执行代码”。V2 选择受信内置适配器注册表，保持清单纯数据。

## 4. 宏观模块

| 模块 | 唯一职责 | 明确不负责 |
|---|---|---|
| 通用配置封装 | 规范化配方 ID、项目显示名、目标目录和配方答案，并生成稳定摘要 | 不理解 Python 包名或 C 标识符 |
| 配方目录 | 注册配方版本、答案模式、默认能力、允许蓝图及受信适配器键 | 不写文件、不运行工具链 |
| 蓝图与能力规划 | 加载声明式蓝图、校验配方边界、求能力闭包、冲突和文件所有权 | 不使用全局语言阶段顺序 |
| 配方装配适配 | 将通用计划和已验证答案转换成候选文件及集中产物 | 不提交目标、不执行验证 |
| 配方验证适配 | 把声明式验证门映射为受控进程规范和行为探针 | 不改变候选、不降低必需门 |
| 通用生命周期 | 会话、锁、摘要凭证、候选工作区、Finalize、恢复和胶囊清理 | 不知道项目语言或框架 |

依赖方向：

```text
通用配置封装 → 配方目录 → 蓝图与能力规划
                         ↘ 配方装配适配 → 配方验证适配
                                              ↓
                                         通用生命周期
```

## 5. 构建顺序

1. 先用契约测试定义通用配置、配方注册和旧 V1 配置兼容映射。
2. 将固定全局阶段替换为配方允许范围内的能力依赖图；顺序依赖必须显式声明。
3. 把现有 FastAPI 集中装配与验证封装进第一套受信适配器，保持四组合输出与验收不退化。
4. 让通用生命周期只依赖配方计划、装配结果和验证报告，不再读取 database、container 或 package_name。
5. 增加最小 C/CMake 配方及声明式蓝图，不修改生命周期与能力求解器。
6. 在 Windows 与 Linux 对两套配方执行真实端到端生成、验证、Finalize 和胶囊清理。
7. 更新用户交互、配置示例、架构说明和发布门禁，再进入 V2 发布候选。

## 6. 架构决策

### ADR-01：使用受信内置适配器注册表

- Decision：配方清单只引用编译器内注册的装配器键和验证器键。
- Context：不同语言确实需要不同的标识符规则、集中产物和真实工具链验证，但任意钩子会破坏安全模型。
- Options：构建完全通用模板 DSL；允许配方执行插件代码；使用受信内置适配器。
- Chosen：受信内置适配器加声明式配方和蓝图。
- Tradeoff：新增语言仍需发布新版编译器，但核心编排无需修改，且清单不能执行任意代码。
- Rollback：若第二套配方证明接口错误，回退注册层，V1 FastAPI 入口继续使用原实现。

### ADR-02：采用通用配置封装与配方答案

- Decision：公共配置只承载配方、显示名、目标目录和 JSON 答案；答案由所选配方严格解析。
- Context：package_name、database、container 都不是跨语言公共字段。
- Options：不断扩大全局配置；每个配方使用完全不同文件；公共封装加配方答案。
- Chosen：公共封装加配方答案，并提供旧 V1 配置到 FastAPI 配方的确定性兼容映射。
- Tradeoff：配置多一层，但消除了全局字段膨胀，并能给未知答案稳定报错。
- Rollback：兼容入口可继续直接构造 V1 配置，不影响已发布胶囊语义。

### ADR-03：移除全局技术栈阶段枚举

- Decision：蓝图顺序由能力依赖图决定；无依赖蓝图使用稳定 ID 排序。
- Context：当前阶段枚举直接编码 Python、FastAPI、PostgreSQL 和 Docker，无法容纳 CMake。
- Options：扩大全局枚举；每个配方定义数字阶段；只使用显式依赖图。
- Chosen：显式依赖图加稳定排序；若顺序影响结果，就必须成为清单依赖。
- Tradeoff：清单需要更准确地表达依赖，但不会出现跨语言的虚假全局顺序。
- Rollback：FastAPI 适配层可在迁移期生成与旧阶段相同的显式依赖关系。

### ADR-04：验证器按配方注册并保持门禁声明式

- Decision：蓝图只声明验证门名称，配方注册表证明每个名称有唯一受信实现。
- Context：现有验证器硬编码 uv、Python、FastAPI、PostgreSQL 和 Docker。
- Options：让蓝图携带命令；建立全局命令字典；配方验证适配器。
- Chosen：配方验证适配器，共享底层受控进程与 Docker 生命周期组件。
- Tradeoff：不能仅复制一个目录就动态新增语言，但不会把用户输入变成命令。
- Rollback：V1 验证器作为 FastAPI 适配器内部实现保留，直到通用调用路径通过全部旧验收。

### ADR-05：采用绞杀式迁移而非重写

- Decision：先把 V1 包装为第一套配方，再逐步把编排迁到通用接口，最后接入 C 配方。
- Context：现有原子交付、自清理和四组合验证已经经过真实 CI，整体重写风险过高。
- Options：原地重写；复制一套 V2；接口包裹后逐层替换。
- Chosen：接口包裹后逐层替换。
- Tradeoff：迁移期会同时存在 V1 命名和通用接口，但每一步都有可运行回滚点。
- Rollback：任何阶段均可让 CLI 继续指向 V1 应用，不迁移 journal 或已完成项目。

## 7. 失效假设

以下任一需求成立时，必须重新评审本蓝图：

1. 新语言必须通过第三方包在运行时动态安装，而不能随编译器发布。
2. 用户必须执行自己的模板脚本或 Shell 钩子。
3. 必须向已有项目增量写入、合并或升级。
4. C 配方无法在受支持 CI 平台提供真实 CMake 和编译器验证环境。
5. 旧 V1 配置可以直接破坏兼容，而不需要迁移期。
6. Finalize 后的项目必须保留配方元数据以便长期管理。

## 8. 蓝图验收条件

宏观方向成立必须同时满足：

- FastAPI 被表达为一个注册配方，而不是核心默认真理；
- C/CMake 接入不修改通用生命周期、Finalize 或胶囊清理；
- 配方与蓝图清单不能执行任意代码；
- 旧 V1 四组合继续通过原有端到端门禁；
- C 项目在 Windows/Linux 真实配置、编译、测试和运行；
- 两种项目 Finalize 后都不包含生成器或生成元数据；
- 任一配方失败都不会创建或污染目标目录。

Blueprint ready. Does the direction look right?

## 9. 已冻结的迁移契约

### 9.1 V2 配置封装

新配置使用显式 schema 与配方答案隔离语言差异：

```json
{
  "schema_version": 2,
  "recipe": "python-fastapi-service",
  "project_name": "Example API",
  "target_directory": "./example-api",
  "answers": {
    "package_name": "example_api",
    "database": "postgres",
    "delivery": "docker"
  }
}
```

C 配方使用同一公共封装，但答案属于自身：

```json
{
  "schema_version": 2,
  "recipe": "c-cmake-cli",
  "project_name": "Example CLI",
  "target_directory": "./example-cli",
  "answers": {
    "target_name": "example_cli",
    "strict_warnings": true
  }
}
```

没有 `schema_version` 与 `recipe` 的现有 V1 配置在迁移期确定性映射到 `python-fastapi-service`。同时出现 V1 平铺字段与 V2 字段时拒绝，不猜测用户意图。配置摘要绑定 schema、配方 ID、配方版本、公共字段和规范化答案。

### 9.2 配方注册契约

每个内置配方注册以下纯数据与受信键：

- 稳定配方 ID 与语义版本；
- 答案模式及默认值；
- 必选能力、可选答案到能力的映射；
- 允许使用的蓝图与验证门集合；
- 编译器内置的答案解析、装配和验证适配器键；
- 用户可读的工具链先决条件。

注册表在启动时验证所有键有唯一受信实现。清单不能包含模块路径、命令参数、代码片段或下载地址。

### 9.3 兼容性门

通用调用路径替换 V1 入口前，四种 FastAPI 配置必须同时满足：

- 计划能力和蓝图集合一致；
- 候选文件集合一致；
- 除已经声明为易变的数据外，文件字节一致；
- 验证门、失败语义和 Finalize 结果一致；
- 旧配置和 V2 配置生成相同配置摘要语义下的等价项目。

## 10. 微任务计划

### Phase 1：通用配置与配方契约

#### Task 1.1｜通用配置封装失败测试

**状态：完成。**

- Input：9.1 配置契约、现有 V1 配置安全规则。
- Output：覆盖 V2 FastAPI、V2 C、V1 兼容映射、混合格式拒绝、未知配方、未知答案、路径安全和稳定摘要的失败测试。
- Risk：全局配置继续泄漏 Python 字段，或兼容层静默误解输入。
- Rollback：只删除新增测试，不触碰 V1 解析器。
- Flag：信任边界；TDD 失败测试。

#### Task 1.2｜通用配置封装最小实现

**状态：完成。**

- Input：Task 1.1 测试。
- Output：语言无关的公共配置值、规范化答案载荷、V1 到 FastAPI 配方的兼容转换。
- Risk：摘要改变使会话凭证失配，或目标路径校验出现两套实现。
- Rollback：CLI 继续调用现有 V1 解析器；新入口尚未接线。
- Flag：不产生文件系统副作用。

#### Task 1.3｜受信配方注册失败测试

**状态：完成。**

- Input：9.2 注册契约与 ADR-01。
- Output：重复配方、未知适配器、无效答案模式、未提供能力、越界蓝图、未知验证门和可执行清单字段的拒绝测试。
- Risk：清单字符串越过数据到代码的边界。
- Rollback：只删除注册契约测试。
- Flag：安全边界；TDD 失败测试。

#### Task 1.4｜FastAPI 配方注册最小实现

**状态：完成。**

- Input：Task 1.3 测试、现有四组合规则。
- Output：静态受信注册表和第一套 `python-fastapi-service` 配方；尚不替换 V1 CLI。
- Risk：默认能力与现有 `postgres + docker` 黄金配置不一致。
- Rollback：删除新注册层，V1 路径不受影响。

### Phase 2：语言无关的蓝图规划

#### Task 2.1｜配方作用域与无阶段规划失败测试

**状态：完成。**

- Input：ADR-03、现有能力 DAG 与文件所有权规则。
- Output：配方蓝图白名单、跨配方能力拒绝、显式依赖顺序、稳定无依赖排序、循环、冲突和重复文件所有者测试。
- Risk：C 与 Python 蓝图混入同一计划，或移除阶段后计划不确定。
- Rollback：保留现有规划器和 schema 1 清单。
- Flag：TDD 失败测试。

#### Task 2.2｜通用计划编译器最小实现

**状态：完成。**

- Input：Task 2.1 测试。
- Output：不依赖语言阶段枚举、受配方允许集合约束的能力计划；计划摘要增加配方身份。
- Risk：旧计划摘要与 journal 恢复语义被提前改变。
- Rollback：新规划器保持旁路，V1 会话继续使用旧计划。

#### Task 2.3｜FastAPI 蓝图 schema 迁移

**状态：完成。**

- Input：Task 2.2、现有七个蓝图及四组合期望。
- Output：移除固定阶段字段的声明式蓝图与等价依赖关系，全部由 FastAPI 配方显式允许。
- Risk：隐式阶段曾掩盖缺失依赖，迁移后顺序或闭包变化。
- Rollback：一次提交内恢复 schema 1 清单和旧加载器。
- Flag：蓝图批量迁移；不改变生成文件。

### Phase 3：受信装配与验证适配层

#### Task 3.1｜通用装配适配器失败测试

**状态：完成。**

- Input：通用配置、配方计划、现有候选装配器。
- Output：适配器唯一注册、答案类型边界、计划/配方绑定、输出所有权和未知适配器拒绝测试。
- Risk：适配器绕过计划写入未声明文件。
- Rollback：只新增旁路接口测试。
- Flag：TDD 失败测试。

#### Task 3.2｜封装 FastAPI 装配实现

**状态：完成。**

- Input：Task 3.1、现有 FastAPI 集中装配与锁文件逻辑。
- Output：FastAPI 受信装配适配器；旧编译函数委托新适配器后保持输出等价。
- Risk：集中产物、锁文件变体或数据库条件代码发生字节漂移。
- Rollback：旧编译函数恢复直接实现。

#### Task 3.3｜通用验证适配器失败测试

**状态：完成。**

- Input：现有受控进程、验证报告、Docker 生命周期和 ADR-04。
- Output：验证门唯一提供者、必需门缺失、未知门、秘密输入、超时和候选改变后的失败测试。
- Risk：配方把声明式门名称变成任意命令，或必需门被 SKIPPED 后仍通过。
- Rollback：V1 验证执行器继续直连。
- Flag：外部进程与秘密；TDD 失败测试。

#### Task 3.4｜封装 FastAPI 验证实现

**状态：完成。**

- Input：Task 3.3、现有 V1 验证器。
- Output：FastAPI 受信验证适配器，复用现有 uv、PostgreSQL 与 Docker 实现。
- Risk：DATABASE_URL 等配方秘密泄漏到公共配置、日志或项目文件。
- Rollback：命令应用继续调用原 V1 验证函数。
- Flag：真实外部进程；日志必须带配方 ID 和门名称。

### Phase 4：通用生命周期接线

#### Task 4.1｜生命周期去语言化失败测试

**状态：完成。**

- Input：通用配置、装配与验证适配器、现有状态机。
- Output：证明会话编排不读取 package、database、container 的契约测试，以及配置/配方/计划/候选摘要绑定测试。
- Risk：表面存在配方，核心命令应用仍然硬编码 FastAPI 分支。
- Rollback：新通用应用保持独立，现有 CLI 不切换。
- Flag：状态机与原子性；TDD 失败测试。

#### Task 4.2｜通用命令应用最小实现

**状态：完成。**

- Input：Task 4.1 与现有生成工作流、Finalize 事务器。
- Output：配方驱动的 preview、run、inspect、discard 编排；复用同一目标锁、journal、Finalize 和清理实现。
- Risk：建立第二套生命周期或让配方决定删除范围。
- Rollback：CLI 保持指向 V1 应用即可完整撤回。
- Flag：候选工作区与 Finalize 副作用。

#### Task 4.3｜CLI 兼容切换

**状态：完成。**

- Input：Task 4.2、V1/V2 配置格式。
- Output：CLI 自动识别明确 schema；旧 V1 配置与命令输出保持兼容，preview 增加配方身份。
- Risk：格式探测含糊或错误回退，使无效 V2 配置被当成 V1。
- Rollback：单点恢复 V1 服务绑定。
- Flag：首次切换用户入口；必须先通过四组合回归。

### Phase 5：C/CMake 参考配方

#### Task 5.1｜C 配方黄金项目失败测试

**状态：完成。**

- Input：C 配置契约、CMake CLI 最小结构和跨平台要求。
- Output：文件树、C 标识符、CMake 配置、严格警告、测试、README、CI 和未选能力零残留的黄金规格。
- Risk：为了快速演示而生成不可维护或只在单平台工作的 C 项目。
- Rollback：只删除 C 黄金规格。
- Flag：TDD 失败测试。

#### Task 5.2｜C 配方与声明式蓝图最小实现

**状态：完成。**

- Input：Task 5.1、通用规划与装配接口。
- Output：`c-cmake-cli` 注册、C 运行时/构建/测试/质量蓝图及受信装配适配器。
- Risk：需要修改通用生命周期才能接入，证明抽象边界失败。
- Rollback：移除整套 C 配方，不影响 FastAPI。

#### Task 5.3｜CMake 验证失败测试

**状态：完成。**

- Input：受控进程接口和 C 黄金项目。
- Output：cmake 不存在、配置失败、编译失败、CTest 失败、运行结果错误、超时和输出上限测试。
- Risk：只检查文件存在却声称 C 项目可用。
- Rollback：只删除 C 验证测试与测试夹具。
- Flag：真实编译器副作用；TDD 失败测试。

#### Task 5.4｜CMake 验证适配器最小实现

**状态：完成。**

- Input：Task 5.3。
- Output：受控的 configure、build、CTest 和产物运行门，构建目录只位于验证环境。
- Risk：构建产物污染候选并使验证后摘要变化。
- Rollback：C 配方保持不可发布，FastAPI 不受影响。
- Flag：外部工具链；必须清理进程树和临时构建目录。

### Phase 6：双配方真实验收

#### Task 6.1｜FastAPI 等价回归矩阵

**状态：完成。**

- Input：通用 CLI 与四种 V1/V2 FastAPI 配置。
- Output：旧配置与 V2 配置的计划、文件、运行行为、Finalize 和自清理等价证据。
- Risk：通用化成功但破坏已经发布的第一套配方。
- Rollback：CLI 回退 V1 绑定，保留新接口继续修正。
- Flag：uv、PostgreSQL、Docker 真实副作用。

#### Task 6.2｜C 双平台端到端验收

**状态：完成。**

- Input：一次性发布胶囊与 C 配方。
- Output：Windows/Linux 从 preview 到编译、CTest、运行、Finalize 和胶囊清理的独立结果。
- Risk：只在开发仓库或单一编译器环境通过。
- Rollback：阻止 V2 发布，不降低平台声明。
- Flag：CMake 与平台编译器真实执行。

#### Task 6.3｜跨配方安全与失败恢复

**状态：完成。**

- Input：两套配方、故障注入和目标竞态场景。
- Output：配方错配、候选篡改、工具链中断、Finalize 竞态、清理失败和重复恢复验收。
- Risk：新适配层绕过现有最强安全保证。
- Rollback：通用 CLI 不发布；V1 继续作为稳定版本。
- Flag：高风险原子性与精确删除。

### Phase 7：文档、审查与 V2 发布门

#### Task 7.1｜用户与配方作者文档

**状态：完成。**

- Input：实际通过验收的两套配方。
- Output：选择流程、V1/V2 配置示例、工具链先决条件、配方/蓝图区别、自清理语义和故障恢复说明。
- Risk：把内置配方误写成可执行第三方插件平台。
- Rollback：阻止发布并修正文档承诺。

#### Task 7.2｜最终工程审查

**状态：完成。**

- Input：完整差异、双平台 CI、故障注入结果。
- Output：按正确性、安全、原子性、可观测性、性能和测试覆盖排序的审查结论。
- Risk：迁移期兼容代码成为永久双轨架构。
- Rollback：按最后一个通过的阶段提交回退，不移动已发布标签。

#### Task 7.3｜V2 发布候选

**状态：完成。**

- Input：无高/中等级审查问题的候选。
- Output：版本升级、确定性胶囊、ZIP、校验文件和 GitHub Release 候选。
- Risk：仅因“能生成两个项目”就跳过清理和恢复门禁。
- Rollback：不创建正式标签；保留 V1 最新稳定 Release。
- Flag：公开发布副作用，必须单独确认。

## 11. 状态快照

- Done：Phase 1–7；FastAPI M-01 到 M-04 的 V1/V2 输入已逐字节得到相同项目；C 配方已在 Ubuntu/Windows 完成真实胶囊全链路；C 工具链中断与发布后清理中断均有安全恢复路径；`v2.0.0-rc.1` 已作为 GitHub prerelease 发布。
- Tests：本地全量为 268 passed、4 skipped；相关装配、适配器、Finalize 与恢复安全回归为 36 passed、20 subtests passed。标签发布 `release-capsule` run 34192475881 的 Ubuntu、Windows 核心质量、FastAPI Linux 四组合、确定性资产构建与 GitHub Release 发布全部成功。ZIP 的两次独立本地构建逐字节一致，SHA-256 为 `eb6a86b0ee5b126d9824f7b59827e3c99559bd6b7f440b6b4251fc14f4fc845b`。
- Next：收集 RC 使用反馈；只有在反馈与回归门禁通过后才评估 `v2.0.0` 稳定版。
- Debt：最低版本已冻结为 CMake 3.20、Ninja 1.10 与兼容 C11 的编译器；托管 Ubuntu/Windows 工具链已验证，具体 GCC/Clang/MSVC 最低版本仍待兼容矩阵冻结。
- Rollback point：移除 CMake 配方注册、四个蓝图、装配/验证适配器与 CLI 工具发现即可回退 Phase 5；FastAPI 与通用生命周期不受影响。

Phase 5 C/CMake reference recipe complete. Proceed with dual-recipe real acceptance?
