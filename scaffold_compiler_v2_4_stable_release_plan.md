# V2.4 稳定版晋升

## 范围与依据

将已验收的 `v2.4.0-rc.1` 冻结为 `2.4.0`。仅修改版本、版本合同和文档；运行逻辑、蓝图、
依赖、生成事务、恢复和自清理保持 RC1 的内容。`v2.3.0` 保留为稳定回滚点。

- RC1 冻结提交：`36cc7ffe9224674e95b847c100baa74ca6cc14b2`。
- RC1 标签工作流 `34996208143` 成功，公开资产验收见 RC1 发布记录。
- RC1 记录提交 `2914d8c` 的工作流 `34996976194` 成功。
- 检查时没有开放 Issue/PR；尚无可确认的外部试用反馈。晋升依据是自动化和本地公开资产
  验收，不将无反馈描述为真实用户验证。

## 发布顺序

1. 统一 `2.4.0` 版本身份，审查与 RC1 的差异。
2. 验证相关版本、配置、胶囊构建和发现合同，完成静态质量门及确定性双构建。
3. 提交版本冻结，等待该提交 Windows、Ubuntu、Linux 真实组合 CI 全绿。
4. 确认公开稳定版发布后，将不可变 `v2.4.0` 标签绑定到已通过 CI 的冻结提交。
5. 标签工作流构建并发布资产，重新下载验证摘要、版本、发现和生成/清理生命周期。
6. 记录工作流与公开资产摘要，更新最新稳定版链接。

公开标签发布会使 `2.4.0` 成为稳定版分发身份；发布后不移动标签或覆盖资产，缺陷进入后续版本。

## 当前状态

版本已冻结为 `2.4.0`；与 RC1 相比，`src` 仅版本常量变化，蓝图无变化。
格式、lint、124 个源文件严格 mypy 通过；相关合同 19 passed、56 个子场景通过。
两次隔离 ZIP 构建字节一致且摘要自校验通过，本地 SHA-256：

```text
4e62de0ed563a7e3e19cdd75f4a3bfe9030145a8e680eb93cc59502d75da3771
```

下一步等待冻结提交的 CI，通过后确认公开稳定版发布。尚未创建稳定标签或公开稳定资产。
