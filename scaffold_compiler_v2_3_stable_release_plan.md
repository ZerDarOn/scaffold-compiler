# Scaffold Compiler V2.3 稳定版发布方案

## 1. 目标与边界

将已经通过精确提交 CI、标签重建和公开下载资产验收的 `v2.3.0-rc.1` 晋升为
`v2.3.0`。稳定版只改变版本身份和发布文档，不改变受信任配方注册、生成字节、验证、
恢复、自清理、蓝图或生成项目内容。

## 2. 七类风险

- Invariants：稳定版运行行为必须与 RC1 一致；包版本、测试合同、胶囊清单、ZIP、摘要和标签
  必须统一为 `2.3.0`。
- Atomicity：版本准备、本地质量、精确提交 CI、标签发布、公开下载验收和证据收尾严格串行；
  任一前置失败都不得创建稳定标签。
- Concurrency：不得并行创建或移动相同标签，不得覆盖已有资产；重复工作流只能产生同摘要资产
  或安全失败。
- Side effects：稳定标签和 GitHub Release 是公开且不可逆的副作用；发布后不移动标签，缺陷
  进入补丁版本。
- Trust boundaries：稳定资产只能由锁定依赖的标签工作流重建；必须从公开 Release 重新下载并
  独立验证 SHA-256，不能用本地目录替代。
- Observability：记录稳定提交、主分支工作流、标签工作流、ZIP 摘要和真实生命周期结果；失败
  日志不得暴露凭据或完整用户环境。
- Performance：稳定版没有运行热路径变化；确定性构建和生命周期审计只属于发布门。

## 3. 发布模块与顺序

1. 资格审查：核对公开 Issue/PR、RC1 资产、近期 CI 和 RC1 后运行代码差异。
2. 稳定版本准备：同步包、测试、验收身份、README 和构建示例。
3. 本地与精确提交门：运行静态、全量、确定性资产验证，并等待该提交的跨平台 CI。
4. 不可变发布：创建 `v2.3.0` 注释标签，由标签工作流重建并发布稳定资产。
5. 独立资产验收：下载 ZIP 与摘要，验证长目标、真实生成、自清理和生成后测试。
6. 证据收尾：记录所有不可变身份，并通过最终主分支 CI。

构建顺序：资格 → 版本准备 → 本地质量 → 精确提交 CI → 明确授权 → 稳定标签 → 下载验收 → 证据。

会使方案失效的假设：RC1 后出现未解决缺陷；稳定准备包含版本和文档以外的运行时变化；
确定性资产不一致；任一精确提交或标签工作流失败。任一成立都停止晋升，不移动既有标签。

## 4. 架构决定

ADR-V23-STABLE-1
  Decision : 从已验收 RC1 运行基线创建独立稳定版本提交和 `v2.3.0` 注释标签。
  Context  : RC1 已证明完整受信任注册、三语言验证、长目标生成和一次性自清理；稳定身份仍需
             保留独立、可审计的候选边界。
  Options  : 移动 RC1 标签；把 RC1 Release 改为稳定；创建只含版本和文档的新提交与标签。
  Chosen   : 创建独立稳定提交，通过精确提交门后再创建不可变稳定标签。
  Tradeoff : 增加一次完整发布链，换取候选、稳定资产和回滚点之间的清晰绑定。
  Rollback : 标签前恢复版本准备；标签后不移动标签，问题进入 `v2.3.1`。

ADR-V23-STABLE-2
  Decision : 采用仓库既有的资产验收门，而不把下载量或固定等待天数设为晋升条件。
  Context  : 本仓库当前是维护者驱动的早期项目，RC1 尚无外部下载；下载量为零不能证明质量，
             也不能提供可重复的技术门。
  Options  : 固定等待；等待外部下载；以精确 CI 和公开资产真实生命周期作为资格证据。
  Chosen   : 沿用 V2.1/V2.2 的可重复技术门，同时保留 RC1 为不可变回滚与审计边界。
  Tradeoff : 无法声称经过广泛用户试用，但不会把时间或偶然下载量误当作工程证据。
  Rollback : 稳定标签前如出现反馈即停止；发布后缺陷进入补丁版本。

## 5. 微任务

[MICRO PLAN]

Phase 1 - 资格与版本
  Task 1.1 | RC1 晋升资格
    Input    : RC1 Release、公开 Issue/PR、近期 CI、RC1 后差异和公开资产验收。
    Output   : 无阻断缺陷且运行代码未漂移的证据。
    Risk     : 已知缺陷晋升、错误基线。
    Rollback : 停止发布。

  Task 1.2 | 稳定版本准备
    Input    : 已验收的 RC1 基线。
    Output   : 包、测试、验收身份、README、构建示例和本方案统一为 `2.3.0`。
    Risk     : 身份不一致、意外运行代码变化。
    Rollback : 标签前恢复版本准备提交。

Phase 2 - 本地与精确提交门
  Task 2.1 | 本地质量与确定性资产
    Input    : 稳定版本准备树与锁定依赖。
    Output   : 格式、lint、mypy、全量测试、双构建和摘要合同通过。
    Risk     : 兼容回归、非确定性资产。
    Rollback : 修复后从头重跑，不发布。

  Task 2.2 | 主分支精确提交 CI
    Input    : 单一稳定版本提交。
    Output   : Windows、Ubuntu 和 Linux 真实发布组合全部成功。
    Risk     : 本地环境掩盖平台差异。
    Rollback : 不创建标签。

Phase 3 - 稳定发布与验收
  Task 3.1 | 标签和 GitHub Release
    Input    : 通过 Task 2.2 的精确提交及明确授权。
    Output   : 不可变 `v2.3.0` 标签、非 prerelease Release 和公开资产。
    Risk     : 公开不可逆副作用。
    Rollback : 不移动标签；问题进入补丁版本。
    Flag     : 需要明确授权创建并推送公共稳定标签。

  Task 3.2 | 公开资产独立验收
    Input    : 从 GitHub Release 下载的 ZIP 和 `.sha256`。
    Output   : 摘要匹配、长目标真实生成、自清理及生成后测试通过。
    Risk     : 供应链绑定、删除安全、平台路径。
    Rollback : 不宣告稳定完成，保留 `v2.2.0` 和 RC1 回滚点。

Phase 4 - 证据收尾
  Task 4.1 | 发布记录
    Input    : 提交、工作流、摘要和生命周期结果。
    Output   : 可复核记录及最终主分支 CI。
    Risk     : 审计证据缺失。
    Rollback : 仅修正文档，公开标签不移动。

Total tasks     : 7
Can parallelize : 公开状态与差异只读检查可并行；所有写入、构建、标签和验收必须串行。

## 6. 状态

- Done：Tasks 1.1–4.1。公开 Issue/PR 均为零；RC1 后只有发布证据文档提交，无运行
  代码漂移；RC1 标签与最终主分支质量门全部成功，公开下载资产摘要和真实生命周期验收通过。
  包、测试、Go 验收身份、README 和构建示例已经统一为 `2.3.0`。
- Tests：141 个文件格式检查、lint、121 个源文件严格 mypy 全部通过；全量测试为
  325 passed、4 skipped，跳过项仅限本机缺失 CMake/Docker 和 Windows 不适用的 POSIX
  symlink 语义。系统临时目录中的 Go 验收首次被 Windows Application Control 阻止；同一
  测试在仓库内受信任临时根通过，完整套件也在该临时根通过，未修改产品或降低门禁。
- Assets：两次隔离构建 ZIP 摘要均为
  `79b4a0f407be16e21fe23a2cc8e05a5a1bb052363e75e93533a047b5aba2da1e`，两个 `.sha256`
  均与实际 ZIP 匹配；清单版本为 `2.3.0`，胶囊 ID 为
  `3de44242-6ec8-4f15-96b6-0acaf13d5606`，临时构建目录已删除。
- CI：精确稳定准备提交 `3b43c7a` 的 quality `34335364517` 已在 Windows、Ubuntu 和
  Linux 四种真实发布组合全部成功。不可变稳定标签精确指向该提交。标签工作流
  `34379349679` 的跨平台质量、Linux 四种真实组合、版本化胶囊构建和 GitHub Release 发布
  也全部成功。
- Release：`v2.3.0` 已作为非 prerelease 稳定版发布：
  <https://github.com/ZerDarOn/scaffold-compiler/releases/tag/v2.3.0>。公开 ZIP SHA-256 为
  `79b4a0f407be16e21fe23a2cc8e05a5a1bb052363e75e93533a047b5aba2da1e`，与本地两次
  确定性构建一致；公开清单版本和胶囊 ID 均匹配。
- Asset audit：从 GitHub Release 独立下载 ZIP 与摘要后校验通过；公开胶囊在 Windows 上以
  89 字符目标目录完成 Go `init`、`preview`、四个验证门和 `FINALIZE`。生成后
  `go test ./...` 通过，胶囊与清理日志均自删除，`.scw-*` 为零，成品中无 scaffold 命名条目，
  临时审计目录已删除。
- Observation：RC1 公开资产当前下载量为零，因此不能声称获得外部用户验证；稳定晋升依据是
  与 V2.1/V2.2 一致的可重复技术门。
- Next：提交本发布证据并等待最终主分支 CI；之后 V2.3 进入稳定维护，后续缺陷使用补丁版本。
- Rollback point：`v2.2.0` 与 `v2.3.0-rc.1` 标签、Release 和资产保持不变。
