# Scaffold Compiler V2.2 短工作区协议方案

## 1. 目标与边界

将生成工作区从 `.<target>.scaffold-<run-id>` 改为目标父目录下固定长度的
`.scw-<32-hex-id>`。这样目标目录名称和自定义运行 ID 不再消耗内部验证路径预算，同时
继续保持候选、验证环境与目标位于同一文件系统，保留原子 no-replace 发布语义。

V2.2 不放宽 120 UTF-16 单元的已验证工作区上限，不移动工作区到系统临时目录，不改变
成品内容、蓝图或外部命令。新版本必须继续安全读取 V2.1 的旧会话证据和旧工作区名称。

## 2. 七类风险

- Invariants：工作区必须唯一绑定目标、运行 ID 和配置摘要；恢复命令不能从被篡改名称推导任意目标。
- Atomicity：工作区仍是目标的同盘兄弟目录；目标发布继续使用原生 no-replace 移动。
- Concurrency：同目标仍由目标锁串行化；短 ID 使用 SHA-256 的 128 位前缀并由排他 mkdir 检测碰撞。
- Side effects：名称选择必须在锁和 mkdir 前完成；拒绝路径仍然零写入。
- Trust boundaries：目标叶名称和运行 ID 均不可信；会话加载、当前名称校验和旧名称解析必须闭合验证。
- Observability：记录运行 ID 与工作区命名方案，不记录完整路径、配置摘要或原始目标名称。
- Performance：每次运行只增加一次有界 SHA-256 和常数次字符串校验，不扫描目录。

## 3. 宏观模块

1. 工作区身份：集中生成固定长度名称，并验证工作区、会话和目标的绑定关系。
2. 会话协议：新会话持久化目标叶名称；读取器兼容旧 schema 1 证据。
3. 生成事务：在第一项副作用前选择短工作区、执行既有路径预算门并记录方案。
4. 恢复事务：inspect、discard 和 cleanup 共享同一绑定校验，旧工作区仍可恢复。
5. 证据与文档：用单元、恢复、验收和文档合同证明路径收益与零误删不变量。

构建顺序：身份失败测试 → 会话兼容测试 → 最小实现 → 生成事务接入 → 恢复接入 →
文档与全量质量门。

会使方案失效的假设：固定名称仍不能覆盖锁定依赖树；短名称需要跨文件系统工作区；
恢复目标不能由会话证据与名称哈希共同绑定；旧 schema 无法无歧义识别。任一成立都回到
架构评审，不能通过放宽清理校验继续。

## 4. 架构决定

ADR-V22-WORKSPACE-1
  Decision : 使用 `.scw-` 加 SHA-256 前 128 位作为当前工作区名称。
  Context  : V2.1 名称重复目标叶名称和完整运行 ID，长目标名会无谓耗尽验证路径预算。
  Options  : 移到全局临时目录；截断目标名；只缩短运行 ID；哈希全部绑定事实。
  Chosen   : 对版本标识、目标叶名称、运行 ID 和配置摘要的规范字节做 SHA-256，取 32 位十六进制。
  Tradeoff : 文件名不再人类可读；失败消息仍给出精确路径，会话与日志保留稳定运行 ID。
  Rollback : 恢复旧名称构造；V2.1 稳定标签不移动。

ADR-V22-WORKSPACE-2
  Decision : 会话 schema 2 增加目标叶名称，读取器继续接受 schema 1。
  Context  : cleanup 必须定位已发布目标，固定哈希名称不能反推出原始目标名。
  Options  : 新增旁路绑定文件；扩展候选所有权；扩展已有会话证据。
  Chosen   : 目标绑定属于运行生命周期事实，存入原子写入且状态机全程保留的会话记录。
  Tradeoff : 持久化 schema 升级；旧证据以 `target_name = null` 进入只读兼容路径。
  Rollback : schema 1 读取路径保留；删除新字段和当前名称分支即可回退。

ADR-V22-WORKSPACE-3
  Decision : 继续执行 120 UTF-16 单元工作区预算，不在本版本提高数值。
  Context  : 该值保护第三方工具的最深锁定依赖路径；短名称解决重复消耗，不改变依赖深度。
  Options  : 同时提高上限；动态扫描依赖；维持已验证上限。
  Chosen   : 维持上限，通过固定工作区叶名称扩大可接受目标名范围。
  Tradeoff : 极深目标父目录仍会被拒绝；换取不削弱 RC2 已验证安全余量。
  Rollback : 无数值迁移；回退名称即可恢复 V2.1 行为。

## 5. 微任务

[MICRO PLAN]

Phase 1 - 身份与会话
  Task 1.1 | 工作区身份失败测试
    Input    : 长目标名、不同运行 ID/配置摘要、伪造名称和路径穿越目标名。
    Output   : 固定长度、确定性、分域、当前绑定和旧名称兼容合同。
    Risk     : 信任边界与碰撞误绑定。
    Rollback : 只删除新增测试。

  Task 1.2 | 会话 schema 失败测试
    Input    : 新目标名、旧 schema 1 JSON、reconfigure 状态转换。
    Output   : schema 2 往返、旧证据读取和目标绑定保持测试。
    Risk     : 已保存失败证据失效。
    Rollback : 只删除新增测试。

  Task 1.3 | 最小身份与会话实现
    Input    : Tasks 1.1–1.2。
    Output   : 纯身份函数和向后兼容会话协议。
    Risk     : 名称规范或反序列化分支不闭合。
    Rollback : 删除身份模块并恢复 schema 1。

Phase 2 - 事务接入
  Task 2.1 | 生成事务失败测试
    Input    : 长目标叶名称和可观察路径预算/锁。
    Output   : 短工作区在任何副作用前选择且不泄露绑定事实。
    Risk     : 原子性和日志隐私。
    Rollback : 只删除新增断言。

  Task 2.2 | 生成事务实现
    Input    : Task 2.1 与身份模块。
    Output   : 所有配方共享短名称并写入目标绑定会话。
    Risk     : 成功清理或失败证据路径漂移。
    Rollback : 恢复单一旧名称构造点。

  Task 2.3 | 恢复事务失败测试与实现
    Input    : 当前/旧工作区、篡改会话、改名目录和相同内容的错误目标。
    Output   : inspect/discard/cleanup 共用严格绑定，歧义时零删除。
    Risk     : 误删或错误清理已发布项目。
    Rollback : 恢复旧解析分支并阻止 V2.2 发布。
    Flag     : 修改显式删除前的所有权判定。

Phase 3 - 产品证据
  Task 3.1 | 验收和文档
    Input    : 三配方生命周期与新失败工作区名称。
    Output   : 用户示例、恢复 glob、长目标名和旧证据合同。
    Risk     : 文档承诺超过实现。
    Rollback : 不发布 V2.2。

  Task 3.2 | 全量与工程审查
    Input    : 完整实现。
    Output   : Ruff、mypy、全量测试、双平台 CI 与发现优先审查。
    Risk     : 平台路径语义或清理边界回归。
    Rollback : 保留 V2.1 stable，不创建 V2.2 标签。

Total tasks     : 8
Can parallelize : 无；身份、会话、生成和删除判定必须串行验证。

## 6. 状态

- Done：Tasks 1.1–3.1；固定身份、会话 schema 2/旧 schema 1 兼容、生成接入、当前/旧恢复、
  错误路径报告、长目标名 Go 胶囊和用户文档均已完成。Go 验收发现并修复 `GOTMPDIR`
  逃逸，所有 Go 构建临时文件现由验证工作区拥有。
- Tests：格式检查 129 个文件、lint、50 个源码文件严格 mypy 和 287 个单元测试通过；此前完整本地
  套件为 308 passed、4 skipped、1 个主机策略失败。失败路径已确认在 owned
  `validation-env/go-tmp`，由 Windows Code Integrity 阻止新编译的未签名 Go 测试 EXE；
  同一长目标名真实胶囊聚焦验收已通过。
- Review：发现并修复 FastAPI 验证运行时仍将旧工作区名称加入绝对路径泄漏检查的问题，新增
  组合根回归测试；当前无未解决的高、中风险发现。
- Done：Task 3.2；提交 `39900f0` 的 GitHub Actions `34317340479` 在 Windows、Ubuntu 和
  Linux 四种真实发布组合上全部通过。
- Release：`v2.2.0-rc.1` 与 `v2.2.0` 均已通过精确提交 CI、标签重建、公开摘要和下载
  资产长目标名生命周期验收；V2.2 已进入稳定维护。
- Debt：120 单元预算仍限制极深父目录；只有新的真实依赖深度证据才能调整。
- Rollback point：`v2.1.0`、`v2.2.0-rc.1` 与 `v2.2.0` 标签和发布资产均不移动。
