# Scaffold Compiler V2.3 RC1 发布方案

## 1. 目标与边界

将已经通过完整受信任配方注册、三语言等价验证和双平台 CI 的 `2.3.0-dev` 主线冻结为
`2.3.0-rc.1`。候选版只改变版本身份、发布记录和构建示例，不再改变注册合同、生成字节、
验证执行、恢复或自清理语义。`v2.2.0` 保持最新稳定版和不可变回滚点。

本方案不会把受信任内置注册开放为第三方运行时插件。动态导入、entry point、清单命令、
下载钩子、任意环境转发、第三方签名和沙箱协议继续明确延期。

## 2. 资格证据

- 公开仓库当前无开放 Issue 或 Pull Request，最新非候选 Release 为 `v2.2.0`。
- `v2.2.0..HEAD` 的运行时变化仅覆盖 V2.3 受信任配方注册方案；没有生成事务、发布、恢复、
  自清理或蓝图语义漂移。
- Phase 3 精确提交 `6f6bdc1` 的 quality `34327983716` 在 Windows 核心、Ubuntu 核心和
  Linux 四种真实发布组合全部成功。
- 最终开发提交 `34a781b` 的 quality `34329004825` 同样全部成功；本地最终单次全量为
  325 passed、4 个环境相关跳过、275 个子场景。

## 3. 七类风险

- Invariants：包版本、测试合同、README 示例、胶囊清单、目录名、ZIP 名和标签必须统一为
  `2.3.0-rc.1`；公开标签只能指向通过精确提交门的版本冻结提交。
- Atomicity：版本冻结、质量门、确定性资产、主分支 CI、标签发布和下载验收严格串行；任一
  前置失败都不得创建公开标签。
- Concurrency：不得并行创建或移动相同标签，不得覆盖已有本地或公开资产；重复工作流只能
  产生同摘要资产或失败关闭。
- Side effects：推送注释标签和 GitHub prerelease 是公开且不可逆的最后步骤；发布后不移动
  标签，缺陷只能进入后续候选版本。
- Trust boundaries：候选资产只能来自锁定依赖的标签工作流；下载资产必须独立验证 SHA-256，
  不能用本地未发布目录代替公开资产验收。
- Observability：记录版本提交、主分支工作流、标签工作流、ZIP 摘要和生命周期审计结果；
  失败不得在日志或文档中记录数据库凭据或其他环境值。
- Performance：版本冻结不改变运行热路径；确定性双构建和真实生命周期仅属于发布门，不进入
  用户生成性能声明。

## 4. 宏观发布蓝图

1. 资格边界：核对公开状态、稳定回滚点和稳定版之后的差异范围。
2. 版本冻结：仅统一候选版本身份、合同测试、文档和构建示例。
3. 本地证据：执行静态质量、全量测试、确定性双构建和摘要自校验。
4. 精确提交门：只接受版本冻结提交的跨平台与真实发布组合 CI。
5. 公开候选：创建不可变注释标签，由标签工作流重建并发布 prerelease 资产。
6. 独立验收：重新下载 ZIP 和摘要，验证长目标、自清理及生成后项目测试。
7. 证据收尾：记录所有不可变身份，主分支最终 CI 通过后结束候选发布。

构建顺序：资格 → 版本冻结 → 本地证据 → 精确提交 CI → 公开候选 → 下载验收 → 记录。

会使方案失效的假设：版本冻结需要运行时代码修复；公开仓库出现阻断问题；确定性双构建摘要
不一致；精确提交 CI 或公开下载生命周期失败。任一成立都返回实现或发布评审，不通过移动标签、
覆盖资产或降低门禁绕过。

## 5. 架构决定

ADR-V23-RC1-1
  Decision : 从 `2.3.0-dev` 创建独立 `2.3.0-rc.1` 版本冻结提交和注释标签。
  Context  : 完整注册改变维护者扩展边界和应用启动接线，适合先经过公开候选资产观察。
  Options  : 直接发布稳定版；继续开发身份；发布 RC1 后再决定稳定晋升。
  Chosen   : 发布 RC1，保持 `v2.2.0` 为最新稳定版。
  Tradeoff : 增加一次标签工作流与公开资产审计，换取清晰的候选回滚和不可变证据链。
  Rollback : 标签前恢复版本冻结；标签后不移动标签，问题进入 `rc.2` 或放弃晋升。

ADR-V23-RC1-2
  Decision : 公开资产只由标签工作流重建，本地资产仅用于确定性资格证明。
  Context  : 直接上传本地文件会使源码提交、锁定依赖、资产和摘要之间缺少可复核绑定。
  Options  : 上传本地资产；复用主分支 artifact；由标签工作流重新构建发布。
  Chosen   : 标签工作流执行完整质量门、构建、摘要校验和 prerelease 发布。
  Tradeoff : 发布时间更长，但资产来源和精确提交身份可审计。
  Rollback : 标签工作流失败时不手工替换资产；修复进入新提交和新候选标签。

## 6. 微任务

[MICRO PLAN]

Phase 1 - 资格与冻结
  Task 1.1 | 公开资格审查
    Input    : Issue、PR、Release、标签和 `v2.2.0..HEAD` 差异。
    Output   : 无阻断项、差异符合 V2.3 范围、稳定回滚点明确。
    Risk     : 信任边界、发布范围。
    Rollback : 出现阻断项即停止。

  Task 1.2 | RC1 版本冻结
    Input    : 已验证的 `2.3.0-dev` 提交。
    Output   : 包、测试、验收身份、README 和作者文档统一为 `2.3.0-rc.1`。
    Risk     : 身份不一致、意外运行时代码漂移。
    Rollback : 标签前恢复版本提交。

Phase 2 - 本地与精确提交门
  Task 2.1 | 静态与全量质量
    Input    : 仅含版本与文档变化的冻结树。
    Output   : 格式、lint、严格 mypy、全量测试通过。
    Risk     : 兼容回归、版本格式失效。
    Rollback : 修复后从头重跑。

  Task 2.2 | 确定性资产
    Input    : 同一冻结树与锁定依赖。
    Output   : 两次胶囊/ZIP 字节一致，两个摘要文件均与实际 ZIP 匹配。
    Risk     : 非确定性、覆盖旧资产。
    Rollback : 使用新临时目录重建；不覆盖、不发布。

  Task 2.3 | 精确版本提交 CI
    Input    : 已提交并推送的冻结 SHA。
    Output   : Windows、Ubuntu 和 Linux 真实发布组合全部成功。
    Risk     : 平台漂移、错误提交被标记。
    Rollback : 不创建标签。

Phase 3 - 公开候选与下载验收
  Task 3.1 | 标签和 GitHub prerelease
    Input    : 通过 Task 2.3 的精确提交。
    Output   : 不可变 `v2.3.0-rc.1` 标签与公开候选 ZIP/摘要。
    Risk     : 公开不可逆副作用。
    Rollback : 不移动标签；缺陷进入后续候选。
    Flag     : 需要明确授权创建并推送公开标签。

  Task 3.2 | 公开资产独立验收
    Input    : 从 GitHub Release 重新下载的 ZIP 和 `.sha256`。
    Output   : 摘要匹配、长目标真实生成、`.scw-*` 清理、胶囊自删除、生成后测试通过。
    Risk     : 供应链绑定、删除安全、平台路径。
    Rollback : 不宣告候选完成；保留 `v2.2.0` 稳定回滚点。

Phase 4 - 证据收尾
  Task 4.1 | 发布记录
    Input    : 提交、工作流、摘要和生命周期结果。
    Output   : 可复核发布记录与最终主分支 CI。
    Risk     : 审计证据缺失。
    Rollback : 仅修正文档，公开标签不移动。

Total tasks     : 7
Can parallelize : 公开状态与差异审查可并行读取；所有写入、构建、标签和验收必须串行。

## 7. 状态

- Done：Tasks 1.1、1.2、2.2；公开 Issue/PR 为零，最新稳定版为 `v2.2.0`。包、测试、
  Go 胶囊验收身份和 README 已统一为 `2.3.0-rc.1`，冻结差异只有身份与文档。两次隔离构建
  ZIP 摘要均为 `18f62e338888741d9e2b43380141efd93a522848742e81a975e89da40adaffdd`，
  两个摘要文件与清单身份均匹配，临时构建目录已删除。
- Tests：121 个 Python 文件格式/lint、121 个文件严格 mypy 通过。完整套件两次均为
  324 passed/4 skipped，唯一失败分别是 Windows 应用控制以 `os error 4551` 间歇阻止
  临时验证环境启动 `mypy`，以及同一宿主随后阻止 Go 工具链子进程；M-01 单独复跑通过，
  Go 单独复跑仍受宿主策略阻断。冻结前相同运行代码曾单次全量 325 passed。
- Review：无版本冻结代码发现；本地 Windows 应用控制是明确的环境残余风险，不能通过修改
  产品代码或降低门禁绕过。精确冻结提交的远端 Windows、Ubuntu 和 Linux CI 将决定资格。
- Next：提交并推送版本冻结，执行 Task 2.3；远端三项未全部成功前不得创建公开标签。
- Debt：第三方插件、签名和沙箱协议不进入 RC1。
- Rollback point：`v2.2.0` 标签、Release 和已验证公开资产保持不变。
