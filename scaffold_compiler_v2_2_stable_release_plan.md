# Scaffold Compiler V2.2 稳定版发布方案

## 1. 目标与边界

将已经通过精确提交 CI、标签重建和公开下载资产验收的 `v2.2.0-rc.1` 晋升为
`v2.2.0`。稳定版只改变版本身份和发布文档，不改变固定长度工作区、会话 schema、恢复、
验证、自清理、蓝图或生成项目内容。

## 2. 风险与不变量

- 稳定版运行行为必须与 RC1 一致；任何运行时代码漂移都阻止晋升。
- 版本、标签和胶囊清单必须一致；稳定标签只能指向通过精确提交 CI 的提交。
- 标签与 GitHub Release 是公开副作用，必须晚于本地质量门和主分支 CI。
- 标签工作流必须重新构建资产；公开下载后独立验证摘要和真实生命周期。
- `v2.1.0` 与 `v2.2.0-rc.1` 均保持不可变，作为回滚和审计边界。

## 3. 发布决定

ADR-V22-STABLE-1
  Decision : 创建只含稳定版本身份和发布记录的独立提交，再创建 `v2.2.0` 注释标签。
  Context  : RC1 已验证持久化会话兼容、短工作区、安全恢复和长目标名真实生命周期。
  Options  : 移动 RC1 标签；把 RC1 Release 改成稳定；创建新的稳定提交与标签。
  Chosen   : 创建新稳定提交和不可变标签，保留完整候选审计链。
  Tradeoff : 增加一个元数据提交，但版本身份和资产来源清晰。
  Rollback : 标签前恢复版本提交；标签后不移动标签，缺陷进入后续补丁版本。

## 4. 微任务

[MICRO PLAN]

Phase 1 - 资格与冻结
  Task 1.1 | RC1 晋升资格
    Output   : 无开放 Issue/PR，RC1 后无运行时代码漂移，公开资产验收通过。
    Rollback : 任一阻断项出现即停止。

  Task 1.2 | 稳定版本冻结
    Output   : 包、测试、验收、README 和构建示例统一为 `2.2.0`。
    Rollback : 恢复版本准备改动。

Phase 2 - 精确提交门
  Task 2.1 | 本地质量门
    Output   : 格式、lint、mypy、测试与确定性资产合同通过。
    Rollback : 不提交或修复后重跑。

  Task 2.2 | 主分支 CI
    Output   : 精确稳定提交在 Windows、Ubuntu 和真实发布组合全部通过。
    Rollback : 不创建标签。

Phase 3 - 稳定发布
  Task 3.1 | 标签与 GitHub Release
    Output   : 不可变 `v2.2.0` 标签、非 prerelease Release 和公开资产。
    Rollback : 不移动标签；问题进入补丁版本。
    Flag     : 创建公开稳定标签和 GitHub Release。

  Task 3.2 | 下载资产验收
    Output   : SHA-256、长目标名生成、自清理和生成后测试证据。
    Rollback : 不宣告完成，保持 `v2.1.0` 为已知稳定回滚点。

Phase 4 - 证据收尾
  Task 4.1 | 发布记录
    Output   : 提交、工作流、摘要和资产验收可复核，最终主分支 CI 全绿。
    Rollback : 仅恢复文档提交，已发布标签不移动。

## 5. 状态

- Done：Tasks 1.1–2.1；公共页面显示开放 Issue/PR 均为零，RC1 后只有证据文档提交，
  公开 RC1 资产摘要、89 字符目标生成、自清理和生成后 Go 测试已通过。稳定版本身份已
  统一；本地 131 个文件格式检查、lint、114 个文件严格 mypy、301 passed/1 个 POSIX
  平台测试跳过，确定性胶囊与摘要合同通过。
- Review：稳定版相对 RC1 的运行代码差异仅为版本常量；无未解决的高、中风险发现。
- Next：Task 2.2，提交并等待精确稳定提交的跨平台 CI。
- Rollback point：`v2.1.0` 与 `v2.2.0-rc.1` 标签均不移动。
