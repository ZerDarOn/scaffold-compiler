# Scaffold Compiler V2.1 RC2 Windows 路径预检方案

## 1. 目标与边界

目标：在 Windows 上于任何锁、工作区、候选或验证环境创建之前，拒绝无法为受控工具和安全回收保留足够路径预算的目标；失败必须零项目写入，并给出可操作的短路径建议。

本次同时补齐 Go schema 2 手写配置示例，并以 `2.1.0-rc.2` 重新完成资产级验收。不会改变蓝图组合、生成文件、验证门、自清理所有权模型或 V2.0 稳定标签。

实测依据：本机 RC1 审计的验证根为 170 个 UTF-16 单元，锁定 FastAPI 依赖的最长相对路径为 107，完整路径达到 278。Python `stat()` 在传统 Windows 260 路径边界附近返回 `FileNotFoundError`，使验证失败且安全 `discard` 执行零删除拒绝。

## 2. 七类风险

- Invariants：不安全路径不能产生锁、隐藏工作区或目标；任何歧义仍必须零删除。
- Atomicity：路径预检必须早于目标锁获取和 `workspace.mkdir()`，否则“提前失败”承诺不成立。
- Concurrency：预检是纯计算；既有目标锁仍负责同一目标的竞争，不新增共享状态。
- Side effects：拒绝路径没有磁盘写入、进程启动或网络访问；正常路径沿用既有事务和补偿。
- Trust boundaries：目标目录和目标叶名称均为不受信任输入；预算以规范化绝对工作区路径的 UTF-16 单元数计算。
- Observability：拒绝记录 run ID、平台和实际/允许长度，不记录完整用户路径；CLI 返回短路径建议。
- Performance：单次 O(路径长度) 计算，不遍历文件系统，不影响验证热路径。

## 3. 宏观模块

1. 平台路径策略：拥有 Windows UTF-16 路径预算、纯计算校验和稳定错误合同。
2. 生成事务前置门：在第一项副作用前调用路径策略，保持所有配方共享同一安全边界。
3. 命令错误边界：区分无工作区的运行时/路径预检失败与已有证据的事务失败。
4. 回归与文档：证明 Windows 拒绝零写入、非 Windows 不受影响，并补齐 Go 配置示例。
5. 候选发布：版本提升为 RC2，执行本地全量、双平台 CI、下载资产摘要与真实生命周期验收。

构建顺序：路径策略测试 → 事务零副作用测试 → 最小实现 → 文档/版本 → 全量与资产验收。

失效假设：如果固定 120 单元仍不能覆盖锁定依赖树，或常见 Windows 项目位置被大量误拒绝，则停止发布并重新评估短工作区命名；不能放宽后继续发布。

## 4. ADR

ADR-WINPATH-1
  Decision : Windows 生成工作区规范化绝对路径最多 120 个 UTF-16 单元。
  Context  : 受控验证工具和依赖仍可能遵循传统 260 路径边界；RC1 在 170 单元验证根下稳定复现失败和不可回收证据。
  Options  : 全面使用扩展路径前缀；将缓存移到外部短目录；缩短工作区协议；事务前保守拒绝。
  Chosen   : 事务前保守拒绝，工作区上限 120；验证根约 135，加实测最长相对路径 107 后约 242，保留约 17 单元余量。
  Tradeoff : 少数深目录用户需选择更短目标路径；换取第三方工具兼容、零残留失败和不改变所有权协议。
  Rollback : 删除平台策略调用即可恢复 RC1 行为；V2.2 可用版本化工作区短命名替代该限制。

ADR-WINPATH-2
  Decision : 不对已有失败证据使用扩展路径绕过或强制删除。
  Context  : 安全清理的首要不变量是歧义时零删除，当前失败目录包含工具生成的深层内容。
  Options  : 强制递归删除；自动加扩展前缀；继续保留并给出人工处置说明。
  Chosen   : 保留证据并阻止稳定版，修复只影响未来运行。
  Tradeoff : 本次审计目录需要用户环境层面的人工清理，但不会扩大删除范围。
  Rollback : 无；这是既有零删除安全合同。

## 5. 微任务

[MICRO PLAN]

Phase 1 - 路径策略
  Task 1.1 | 失败测试
    Input    : Windows/非 Windows 平台名、短/长规范化工作区路径。
    Output   : UTF-16 边界、稳定错误、非 Windows 兼容测试。
    Risk     : 信任边界、跨平台误拒绝。
    Rollback : 删除新增测试。

  Task 1.2 | 最小实现
    Input    : Task 1.1。
    Output   : 纯计算路径预算验证器和结构化日志。
    Risk     : 路径长度算法与 Windows 语义不一致。
    Rollback : 移除策略模块。

Phase 2 - 事务接入
  Task 2.1 | 零副作用测试
    Input    : 超预算目标、可观察锁存储和计划编译器。
    Output   : 证明拒绝发生在锁、计划和工作区创建之前。
    Risk     : 原子性、副作用。
    Rollback : 删除事务回归测试。

  Task 2.2 | 前置门接入
    Input    : Task 2.1 与实际 run ID。
    Output   : 所有配方共享的事务前置校验。
    Risk     : 改变既有成功路径或错误类型。
    Rollback : 移除单一接入点。

Phase 3 - 文档与发布
  Task 3.1 | 用户合同与版本
    Input    : 路径错误合同、Go schema 2 字段、RC1 证据。
    Output   : Windows 短路径建议、Go JSON 示例、`2.1.0-rc.2`。
    Risk     : 文档承诺超过实现。
    Rollback : 恢复文档和版本。

  Task 3.2 | 全量与候选发布
    Input    : 全部实现与测试。
    Output   : 本地全量、Windows/Ubuntu CI、RC2 prerelease、下载资产摘要和真实 Go/FastAPI 生命周期证据。
    Risk     : 发布资产与开发树不等价。
    Rollback : 不打 stable；RC1 和 V2.0 标签不移动。
    Flag     : 创建公开 Git 标签与 GitHub prerelease。

Total tasks     : 6
Can parallelize : 文档草案可与核心测试并行；实现、版本和发布必须串行。

## 6. 状态

- Done：Tasks 1.1–3.2；路径策略、事务前置门、结构化拒绝日志、准确的无工作区错误边界、Go 配置示例、RC2 版本与候选发布均已完成。
- Tests：本地全量 296 passed、4 skipped、246 subtests passed；本地 RC2 胶囊证明缺工具和超长路径均不虚构工作区，超长路径拒绝无锁、无目标、无隐藏工作区。
- CI：主分支 quality `34244947485` 与标签 release-capsule `34245407766` 全部通过；后者的质量、构建和发布五个任务均成功。
- Release：`v2.1.0-rc.2` 已作为 prerelease 发布，下载胶囊 SHA-256 为 `dcfe9170e2ce2322085fcdde1aba83fa4f76aaa68f6efe20635f247d7f95984a`。
- Asset audit：下载 ZIP 摘要匹配；下载胶囊的长路径零副作用拒绝和 Go 发布、自清理、生成后测试均通过。
- Review：无剩余高/中等级发现；完整路径不进入日志，策略为 O(路径长度) 且发生在第一项磁盘副作用之前。
- Next：保留 RC2 观察窗口；没有新缺陷时单独评审 V2.1 stable。
- Debt：120 单元为兼容性护栏；V2.2 可评审版本化短工作区命名以减少限制。
- Rollback point：`v2.1.0-rc.1` 与 `v2.0.0` 均保持不可变。
